// Method array yang sering dipakai
// join adalah method untuk menggabungkan array menjadi string
// slice adalah method untuk memotong array
// reverse adalah method untuk membalik array

const students = ["Eko", "Kurniawan", "Khannedy"];

const join = students.join(",");
console.log(join);

const slice = students.slice(1, 3);
console.log(slice);

const reverse = students.reverse();
console.log(reverse);
